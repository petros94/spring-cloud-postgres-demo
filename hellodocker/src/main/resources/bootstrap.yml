spring:
  application:
    name: hellodocker
  cloud:
    config:
      uri: http://${configserver.host:localhost}:${configserver.port:8888}/
    profiles:
      active: default

  datasource:
    url: jdbc:h2:mem:testdb